# ISL raw data directory
# JSON files collected from mobile app go here
# Format: {gesture_name}.json (e.g., NAMASTE.json, HELLO.json, ...)
# Each file contains metadata + array of 30-frame landmark sequences
