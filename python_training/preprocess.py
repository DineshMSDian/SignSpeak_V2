# preprocess.py
# Normalize + augment collected landmark data
# Loads JSON files from data/asl_raw/ and data/isl_raw/
# Applies: wrist-relative normalization, scale normalization
# Augments with: horizontal flip + gaussian noise (3x data)
# Saves processed numpy arrays to data/processed/
#
# Usage: uv pip install numpy
#        python preprocess.py

# TODO: Implement
